const linkbtn = document.querySelector("link-btn");
const links = document.querySelector("links");
linkbtn.addEventListener("click", (e) => {
    icon.classList.toggle("show-more");
    if (linkbtn.innerHTML ==="<span class="icon">
    <span></span>
    <span></span>
    <span></span>
</span>")
}) 